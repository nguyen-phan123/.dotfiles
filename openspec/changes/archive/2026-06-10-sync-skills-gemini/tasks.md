## 1. Implementation

- [x] 1.1 Update `shell/zsh/.sync-mcp.js` to implement the skills symlinks sync and prune logic.

## 2. Verification

- [x] 2.1 Run `mcpsync` to verify that both MCP settings and agent skills symlinks are synchronized correctly.
